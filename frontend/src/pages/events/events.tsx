import {JSX} from "react";
import {Offer} from "../../models/offer";

export default function EventPage(): JSX.Element {
    return (
        <div className="">
            <h1>Evenements</h1>
        </div>
    );
}